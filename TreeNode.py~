"""
Contains the implementation of a Node of a Red-Black tree
"""

### helper enum implementation ###
def enum(**enums):
    return type('Enum', (), enums)
##################################

COLOR = enum(RED=0, BLACK=1)
